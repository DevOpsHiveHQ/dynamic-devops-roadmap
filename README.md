# HiveBox Project

HiveBox is an API to track environmental sensor data for beekeepers.

## Getting Started

### Prerequisites
* Docker installed

### Running the App

1. Build the Docker image:
   ```bash
   docker build -t hivebox:v0.0.1 .
   ```

2. Run the container:
   ```bash
   docker run hivebox:v0.0.1
   ```
